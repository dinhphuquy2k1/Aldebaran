export enum DiscountProductType {
    ALL = 0,
    GROUP = 1,
    PRODUCT = 2,
    VARIANT = 3,
}

export enum DiscountMinConditionType {
    NONE = 0,
    ORDER_VALUE = 1,
    QUANTITY = 2,
}

export enum DiscountCustomerType {
    ALL = 0,
    GROUP = 1,
    CUSTOMER = 2,
}

export enum DiscountChannelType {
    ALL = 0,
    CUSTOM = 1,
}

export enum DiscountBranchType {
    ALL = 0,
    CUSTOM = 1,
}

export enum DiscountRegionType {
    ALL = 0,
    CUSTOM = 1,
}
